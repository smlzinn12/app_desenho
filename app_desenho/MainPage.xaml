<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="app_desenho.MainPage"
             Title="Home"
             BackgroundColor="#FF3535">

    <ScrollView>
        
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            BackgroundColor="#FC6C85">
            <Label
                Text=" "
                FontSize="26"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                Margin="0,15,0,5" />
            <Image
                Source="mcqueen.png"
                HeightRequest="200"
                Aspect="AspectFit"/>

            <Label
                Text="Seja bem-vindo"
                FontSize="26"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                TextColor="White"
                 />

            <Label
                Text="Abra o menu lateral e escolha o personagem que quer conhecer"
                FontSize="14"
    HorizontalOptions="Center"
    HorizontalTextAlignment="Center"
    TextColor="White"
      />

            
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
